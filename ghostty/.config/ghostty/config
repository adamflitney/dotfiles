# Ghostty Configuration
# https://ghostty.org/docs/config

# Enable shell integration for window titles
# This allows tmux to set the window title via set-titles
shell-integration-features = title

# Always start tmux - attach to existing session or create new one
# This ensures there's always a tmux client for sesh to work with
command = /opt/homebrew/bin/tmux new-session -A -s main

# Font configuration (adjust to your preference)
# font-family = "JetBrains Mono"
# font-size = 14

# Theme (uncomment to use)
# theme = tokyonight

# Window settings
# window-decoration = false
# macos-titlebar-style = hidden
window-height = 40
window-width = 120
