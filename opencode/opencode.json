{
  "$schema": "https://opencode.ai/config.json",
  "instructions": [
    "When cloning git repositories, clone them to ~/dev/ unless the user specifies a different location."
  ],
  "plugin": ["@tarquinen/opencode-dcp@latest"],
  "mcp": {
    "coto": {
      "type": "local",
      "command": ["coto-mcp"]
    }
  }
}
